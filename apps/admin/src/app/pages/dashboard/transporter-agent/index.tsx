import { FC } from "react";
import { Outlet } from "react-router-dom";

export const DashboardTransporterAgentProvider: FC = () => {
  return <Outlet />;
};

export default DashboardTransporterAgentProvider;

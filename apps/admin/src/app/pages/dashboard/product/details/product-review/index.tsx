import { FC } from "react";
import { Outlet } from "react-router-dom";

export const DashboardProductReviewProvider: FC = () => {
  return <Outlet />;
};

export default DashboardProductReviewProvider;

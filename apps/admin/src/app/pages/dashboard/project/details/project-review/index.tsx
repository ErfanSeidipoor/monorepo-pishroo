import { FC } from "react";
import { Outlet } from "react-router-dom";

export const DashboardProjectReviewProvider: FC = () => {
  return <Outlet />;
};

export default DashboardProjectReviewProvider;

import { FC } from "react";
import { Outlet } from "react-router-dom";

export const DashboardTransporterActionProvider: FC = () => {
  return <Outlet />;
};

export default DashboardTransporterActionProvider;

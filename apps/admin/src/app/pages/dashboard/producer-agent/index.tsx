import { FC } from "react";
import { Outlet } from "react-router-dom";

export const DashboardProducerAgentProvider: FC = () => {
  return <Outlet />;
};

export default DashboardProducerAgentProvider;
